generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model t_test {
  id   Int     @id @default(autoincrement())
  col1 String?
}
